import { CurrentDirectorsPipe } from './current-directors.pipe';

describe('CurrentDirectorsPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentDirectorsPipe();
    expect(pipe).toBeTruthy();
  });
});
